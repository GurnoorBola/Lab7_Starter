# Lab 7
### Participants: 
* Gurnoor Bola

## Check your understanding
1. I would put the automated tests within a Github action that runs whenever code is pushed because it ensures that bad code that doesn't pass tests never gets merged. It also runs instantly so I don't have to worry about manually getting out of my IDE to go to a terminal and run tests.
2. No. You would not use an end to end test for that. You would use a unit test.
3. The difference between navigation mode and snapshot mode is that navigation mode evaluates how good the site is when it does a fresh start and snapshot mode evaluates how good the site is running currently.
4. We could avoid chaining critical requests,  defer or inline network requests to move them out of the critical path, and we can use efficient cache lifetimes.





